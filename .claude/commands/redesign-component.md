Redesign the component at `$ARGUMENTS`.
 
Steps:
1. Use the ui-designer agent to create the new design spec (responsive wireframe, Bootstrap classes, SCSS)
2. Use the frontend-builder agent to implement the new design
3. Verify: all existing props still work, all event handlers preserved, responsive at all breakpoints
 
Show the before (current) and after (new) HTML structure.
