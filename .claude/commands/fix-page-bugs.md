Fix all bugs found on the page at `frontend/src/pages/$ARGUMENTS`.
 
Steps:
1. Read the page and all its imports
2. Identify every bug (API URLs, null checks, missing states, broken imports)
3. Fix each bug — show what changed and why
4. Verify: no new console errors, all features still work
5. List every fix made with before/after
 
Do NOT redesign the UI in this step — only fix functional bugs.
