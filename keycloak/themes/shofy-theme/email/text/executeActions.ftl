${msg("executeActionsGreeting")} ${user.firstName!user.username},

${msg("executeActionsDescriptionText", realmName, requiredActionsText)}

${link}

${msg("executeActionsExpiry")} ${linkExpirationFormatter(linkExpiration)}.

${msg("executeActionsIgnore")}
